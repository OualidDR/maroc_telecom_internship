name: 'nidd_dw'
version: '1.0.0'
config-version: 2

profile: 'nidd_dw'

model-paths: ["models"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  nidd_dw:
    staging:
      +materialized: view
    marts:
      +materialized: table
