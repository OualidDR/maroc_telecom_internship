"""Serving-side pipeline: prediction API client + the Spark predict-and-Gold job.

Package marker so `serving.predict_client` resolves for tooling/imports (the
scripts also add the repo root to sys.path at runtime).
"""
